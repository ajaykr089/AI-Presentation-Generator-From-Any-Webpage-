// Background script for AI Presentation Generator extension

chrome.runtime.onInstalled.addListener(() => {
  console.log('AI Presentation Generator extension installed');
});

// Handle any background tasks if needed
